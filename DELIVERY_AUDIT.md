# سجل فحص التسليم

## ما تم فحصه محلياً
- نجح `node scripts/verify-source.mjs` في فحص بنية المصدر واللغة والبيانات المنظمة وإعداد النطاق والقيم المحظورة.
- بنية المشروع والملفات المطلوبة موجودة.
- لا يوجد `pnpm-workspace.yaml` (المشروع حزمة واحدة).
- اللغة المرئية للموقع عربية `ar-OM` واتجاه RTL.
- الصفحات القانونية مستقلة: `/privacy/` و`/terms/` و`/cookies/`.
- معرف GA4 مضبوط على `G-HXM22WWPKP` ولا يتم تحميله قبل موافقة التحليلات.
- لا توجد نطاقات تجريبية أو عناوين محلية أو بروتوكولات إضافات متصفح ضمن صفحات الموقع.
- خريطة جوجل مضبوطة على العربية/عُمان.
- sitemap مشروط بوجود `SITE_URL`.
- نقطة دخول Cloudflare Worker هي `@astrojs/cloudflare/entrypoints/server` وفق نمط Astro الحديث.
- توثيق ربط الكيان موجود في `ENTITY_ASSOCIATION.md` والمراجع في `SOURCES.md`.

## قيد بيئة التنفيذ
بيئة الإنشاء الحالية لا تستطيع الوصول إلى `registry.npmjs.org`، وCorepack يفشل قبل تنزيل pnpm، لذلك تعذر تنفيذ `pnpm install --frozen-lockfile` و`pnpm check` و`pnpm build` فعلياً هنا. ملف القفل المرفق يثبت مواصفات الاعتمادات المباشرة فقط وليس قفلاً انتقالياً مكتملاً، ويجب إعادة توليده في بيئة متصلة قبل اعتماد شرط frozen-lockfile.

لا يُدّعى في هذا التسليم أن فحص Astro أو البناء قد نجح.

## الصور
الصور المختارة صور فوتوغرافية حقيقية مرتبطة بسوق مطرح وكورنيش مطرح وموثقة بالتراخيص. تعذر نسخ ملفات الصور الثنائية إلى الحزمة بسبب حظر التنزيل الخارجي في بيئة التنفيذ، لذلك تستخدم النسخة الحالية روابط ويكيميديا المباشرة وتضم ملفاً مستقلاً بمصادر الصور. هذه نقطة غير مطابقة للرغبة في الاستضافة المحلية الكاملة، ولا يتم إخفاؤها في تقرير التسليم.

## تحديث 2026-09-03 (توسعة + إصلاح CI)
- أُعيد توليد `pnpm-lock.yaml` كاملاً في بيئة متصلة (كان جزئياً بلا قسم `packages`)، وتُثبَّت الاعتمادات الآن بنجاح عبر `pnpm install --frozen-lockfile`؛ كما أُنفذ فعلياً `astro check` (0 أخطاء) و`astro build` بنجاح.
- أُضيفت أقسام دون حذف أي محتوى سابق:
  - `#history` التاريخ والمكانة التجارية.
  - `#facilities` المرافق والخدمات (أنواع فقط، بلا أسماء تجارية).
  - `#weather` الطقس الحالي + توقعات 7 أيام عبر Open-Meteo مع تخزين مؤقت (Cache API في Workers + ذاكرة داخل العملية) وبطاقة بديلة عند الفشل.
  - `#route` اقتراح جولة.
  - الأسئلة الشائعة: من 4 إلى 6 أسئلة (متزامنة مع FAQPage Schema).
- الصور: أصبحت مستضافة محلياً بالنسخ المضغوطة (19.6 ميغابايت ← نحو 1.3 ميغابايت، `pnpm optimize:images`)، ووثقت الأسماء المحلية في `IMAGE_SOURCES.md`؛ بقيت `og:image` على رابط ويكيميديا المطلق إلى حين ضبط النطاق.
- `pnpm-workspace.yaml` أُضيف كملف إعدادات فقط (`allowBuilds` لـ esbuild/workerd؛ متطلب pnpm 11) **بدون** `packages:`، وبقيت البنية وحيدة الحزمة؛ عُدّل `verify-source.mjs` و`verify-build.mjs` وفقاً لذلك (هذا الأخير لا يفحص `dist/server` لأنه يحتوي كلمات إطار العمل مثل example.com وlocalhost).

## تحديث 2026-09-03 (النطاق + ربط الكيان + PWA)
- **النطاق**: ضُبط `SITE_URL = 'https://muttrahsouk.com'` في `astro.config.mjs` (المصدر الوحيد)، فأصبح البناء يولّد `sitemap-index.xml`/`sitemap-0.xml` (4 روابط بلا lastmod) وcanonical وروابط OG مطلقة تلقائياً عبر `Astro.site`؛ وحدّث `verify-source.mjs` ليتوقع الثابت المضبوط، و`public/robots.txt` يوجّه إلى sitemap.
- **ربط الكيان (نموذج Single-Attraction)**: أضيف `@id #attraction` و`image` (نسخة محلية مطلقة) إلى TouristAttraction، مع توسيع `alternateName` و`description` ليشمل الدولة والمعالم القريبة؛ العنوان صار `سوق مطرح (مسقط) | دليل الزيارة والموقع`؛ أُضيف عنصر التسلسل الجغرافي (عُمان › محافظة مسقط › مطرح › سوق مطرح)، ومقال الافتتاح يقرن الاسم العربي بـ Mutrah/Muttrah Souq والموقع الجغرافي؛ `og:image:alt` و`twitter:image` أُضيفا إلى BaseLayout؛ بطاقة التقييم تعرض `4.4 / 5 · 30,121`؛ المصادر الرسمية (وزارة التراث والسياحة وبلدية مسقط) أُضيفت كروابط إلى تذييل الصفحة. خريطة جوجل بقيت بالعربية/عُمان (`!1sar!2som`) التزاماً بفحص التوطين.
- **PWA**: `site.webmanifest` اكتمل (id/start_url/scope/lang ar-OM/dir rtl/أيقونات 32/180/192/512)، ووُلّد `icon-192.png` و`icon-512.png` من أيقونة اللمس؛ أُنشئ `public/sw.js` (شبكة أولاً + بديل دون اتصال للصفحة الرئيسية) مع تسجيل تلقائي في `BaseLayout.astro`.
- **التحقق**: `astro check` = 0 أخطاء (1 hint سابق لـ jsonLd)، `astro build` نجح (sitemap-index في dist/client)، والفحص المكافئ لـ verify-source/verify-build محلياً = AUDIT OK. النقطة القديمة «بقيت og على ويكيميديا» أصبحت لاغية: OG الآن على النسخة المحلية المطلقة بعد ضبط النطاق (رابط ويكيميديا احتياطي فقط).
- ملاحظة: الأرقام (4.4 / 30,121 / 09:00–23:00 / +968 9827 7478 / JHC7+5Q / رابط الخريطة والـ embed) كلها محدّثة وفق أحدث بيانات المستخدم؛ مع بقاء الحاجة لمراجعة عربية نهائية من متحدث أصلي.

## تحديث 2026-09-03 (إصلاحات ما بعد تدقيق الامتثال)
- أُصلح خطأ مسار Windows في `verify-source.mjs` و`verify-build.mjs` (كانا يستخدمان `.pathname` فينتج مسار مزدوج `C:\C:\...` فيتعذر تشغيلهما على Windows). الاستبدال عبر `fileURLToPath(new URL('..', import.meta.url))`؛ تحقق فعلي على Windows: «Source audit passed.» و«Build audit passed.».
- أُزيل تحذير jsonLd الوحيد في `astro check` بإضافة `is:inline` إلى وسم JSON-LD في `BaseLayout.astro`.
- أُضيفت صفحة 404 مخصصة `src/pages/404.astro` (عربية موحدة مع هوية الموقع)؛ في وضع SSR ترجعها Astro تلقائياً للطرق غير المطابقة.
- أُضيف شريط موافقة ملفات تعريف الارتباط `src/components/CookieBanner.astro`: يظهر فقط قبل أول قرار؛ «الضرورية فقط» يحفظ `necessary` و«قبول ملفات التحليل» يحفظ `analytics` ثم يطلق حدث `consent-updated` لتحميل GA4 فوراً دون إعادة تحميل الصفحة، مع رابط إلى `/cookies/` لإدارة تفصيلية.
- `AnalyticsConsent.astro` يستمع الآن إلى `consent-updated` إضافة إلى الفحص عند التحميل (يبقى الافتراضي: لا تحميل بدون موافقة).
- تنظيف: حُذفت ملفات مؤقتة (`devlog.txt`/`devlogerr.txt`/`devpage.html`) خلفتها تجربة خادم معاينة.
- التحقق: `astro build` نجح، و`verify-build.mjs` بعد البناء = Build audit passed، وread_lints 0 خطأ في الملفات المعدلة. `astro check` لم يُعد تنفيذه هنا (تخطّي المستخدم) ويُنصح بتشغيله في CI.
